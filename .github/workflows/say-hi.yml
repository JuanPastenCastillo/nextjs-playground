name: Dummy YML

on:
  push:
    branches:
      - main

jobs:
  say_hi:
    runs-on: ubuntu-22.04
    steps:
      - name: Display some text like Hi
        run: |
          echo "First dummy YML, work?"
      - run: date
      - run: ls -l
